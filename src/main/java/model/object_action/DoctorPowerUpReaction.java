package model.object_action;

import com.google.inject.Inject;
import model.BoardCell;
import model.board_object_instances.Heap;

public class DoctorPowerUpReaction  implements CollisionReaction {

    @Override
    public void handleCollision(BoardCell boardCell) {

// TODO: implement
        //        There we need to remove powerup from the cell
//        And notify the game that the powerup has been added to the player

    }
}
